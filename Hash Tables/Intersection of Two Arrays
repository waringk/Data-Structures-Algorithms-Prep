class Solution(object):
    def intersection(self, nums1, nums2):
        """
        :type nums1: List[int]
        :type nums2: List[int]
        :rtype: List[int]
        
        """
        hash_set1 = set(nums1)
        hash_set2 = set(nums2)

        return [i for i in hash_set1 if i in hash_set2]

https://leetcode.com/problems/intersection-of-two-arrays/
