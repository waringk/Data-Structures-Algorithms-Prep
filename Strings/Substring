s1 = 'laboratory'
s2 = 'rat'
Write a function that takes in two strings and returns true if the second string is substring of the first, and false otherwise.


Sliding Window

def find_substring(str1, str2):
  window = len(s2)
  i = 0
  visited = 0
  while (visited + window) < len(s1):
    cur = s1[i:i+window]
    if cur == s2:
      return True
    visited += 1
    i += 1
  return False



print(find_substring(s1, s2))
